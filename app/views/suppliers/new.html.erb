<h2>Cadastrando um novo galpão</h2>

<% if @supplier.errors.any? %>
  <p>Verifique os erros abaixo:</p>
  <ul>
  <% @supplier.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>

<%= render('form') %>

<%= link_to 'Cancelar', suppliers_path %>