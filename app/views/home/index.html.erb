<% @warehouses.each do |warehouse|%>
  <h3><%= link_to warehouse.name, warehouse_path(warehouse.id) %></h3>

  <ul>
    <li><p><%= warehouse.code %></p></li>
    <li><p><%= warehouse.city %></p></li>
    <li><p><%= warehouse.area %></p></li>
  </ul>
<% end %>

<% if @warehouses.empty? %>
  <p>Não existem galpões cadastrados.</p>
<% end %>